//route to POST "/api/friends" that handles all incoming survey results and compatability logi



//a POST route "/api/friends" that handles all incoming survey results and also handles the compatability logic